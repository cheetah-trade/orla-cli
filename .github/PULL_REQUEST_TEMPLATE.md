<!-- What changes, and why it belongs in the client rather than in the service. -->

## What this changes

## How it was checked

<!-- `npm run check` is the floor. Say which command you actually ran against a
     live connection, and what it printed. -->

- [ ] `npm run check` is clean
- [ ] Ran the changed command against a real connection
- [ ] Commits are signed off (`git commit -s`)
